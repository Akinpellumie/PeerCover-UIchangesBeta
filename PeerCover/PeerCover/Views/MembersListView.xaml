<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PeerCover.Views.MembersListView">
    <ContentView.Content>
        <StackLayout Margin="10,10,10,10" Orientation="Vertical">
            <Label HorizontalOptions="StartAndExpand" Text="{Binding fullname}" TextColor="{StaticResource TxtColor}" 
                                               FontSize="12" x:Name="MemberName"/>
            <Label FontSize="8" Text="{Binding priviledges}" Margin="0,3,0,0"
                                        TextColor="{StaticResource BtnColor}" HorizontalOptions="StartAndExpand"
                                         x:Name="AdminRole"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>